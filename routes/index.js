import express from "express";
const app = express();
// const admin = require("../middleware/authorization/admin");
// const cis_auth = require("../middleware/authorization/client_id_secret");
// const admin_controller = require("./admin/_controller");
// const user_controller = require("./user/_controller");

// app.use("/api/bgd/v2/admin", cis_auth, admin, admin_controller);
// app.use("/api/bgd/v2/user", cis_auth, user_controller);

export default app;
